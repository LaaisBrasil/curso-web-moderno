/* interessante que se encontre números
durante a programação que nao vão mudar 
chamados de : constantes - const */

const a = 4
const b = 5

console.log( a + b);
console.log(a * 3 + (b - 10))

/* resumindo, se cria mais constantes durante um 
projeto do que variaveis, nao que nao va existir variaveis,
mas serão poucas comparadas as constantes */
